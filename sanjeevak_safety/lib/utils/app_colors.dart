import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xFFFF5C5C); // Soft red for primary actions
  static const secondaryColor = Color(0xFFFF8A80); // Light red for secondary actions
  static const backgroundColor = Color(0xFFF3F3F3); // Light, neutral background
  static const surfaceColor = Color(0xFFFFFFFF); // White for cards and surfaces
  static const textColor = Color(0xFF212121); // Darker gray for primary text
  static const subduedTextColor = Color(0xFF757575); // Medium gray for secondary/subtle text
  static const borderColor = Color(0xFFE0E0E0); // Light gray for borders
  static const successColor = Color(0xFF4CAF50); // Green for success messages
  static const warningColor = Color(0xFFFFC107); // Yellow for warnings
  static const errorColor = Color(0xFFF44336); // Bright red for errors
}
